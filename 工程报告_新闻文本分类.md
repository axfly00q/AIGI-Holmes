# 机器学习大作业工程报告

## 基于 AIGI-Holmes 的新闻文本分类系统设计与实现

姓名：  
学号：  
专业：  
课程：机器学习  

## 摘要

随着互联网新闻数量快速增长，新闻文本自动分类在新闻检索、内容推荐、舆情分析和媒体审核等场景中具有重要应用价值。传统人工分类方式效率较低，难以满足大规模新闻数据实时处理需求。针对机器学习大作业第 11 题“新闻文本分类”的要求，本工程以已有的 AIGI-Holmes 新闻图片 AI 生成检测系统为基础，新增新闻文本分类模块，将原本侧重于新闻图片真实性检测的系统扩展为同时支持图片检测和文本分类的新闻内容分析平台。

本项目完成了从数据准备、文本预处理、特征提取、模型训练、实验对比、模型评估到系统集成的完整流程。数据方面，本项目整理并使用了财经、体育、科技、娱乐、社会、教育、军事、汽车共 8 类新闻文本，最终形成 20000 条均衡训练样本。模型方面，本项目没有只训练单一模型，而是比较了朴素贝叶斯、逻辑回归、线性 SVM 和 TextCNN 四类模型，并通过 Accuracy、Precision、Recall、Macro F1 和混淆矩阵等指标进行评估。系统方面，本项目将训练好的模型集成到 FastAPI 后端和 Web 前端中，实现了单条新闻分类、CSV 批量分类、模型指标展示、实验分析展示和用户历史记录保存等功能。

实验结果表明，在当前数据集和特征方案下，朴素贝叶斯模型取得了较好的综合表现，Accuracy 为 0.8050，Macro F1 为 0.8053；TextCNN 模型也取得了接近的效果，Accuracy 为 0.8043，Macro F1 为 0.8024。最终系统能够根据新闻标题和正文摘要自动输出新闻类别、置信度、各类别概率分布和关键词，达到了新闻文本分类系统的基本工程要求。

关键词：新闻文本分类；机器学习；TF-IDF；朴素贝叶斯；TextCNN；FastAPI；AIGI-Holmes

# 一、研究背景

## 1.1 研究领域与问题来源

新闻文本分类是自然语言处理和机器学习中的典型监督学习任务，其目标是根据新闻标题、正文或摘要，将新闻自动划分到预定义类别中。例如，一篇关于股票、银行、汇率的新闻通常应划分为财经类；一篇关于比赛、球队、运动员的新闻通常应划分为体育类；一篇关于人工智能、芯片、手机或互联网的新闻通常应划分为科技类。

在实际应用中，新闻文本分类具有较高的实用价值。新闻网站需要根据类别组织栏目；搜索引擎需要根据主题建立索引；推荐系统需要根据用户兴趣推送不同类型新闻；内容审核系统也需要先识别新闻主题，再进行更细粒度的真实性、敏感性或安全性分析。因此，新闻文本分类既是机器学习课程中适合练习特征提取和分类算法的任务，也是现实新闻内容处理系统中的基础模块。

本工程所选题目为“新闻文本分类”。题目要求从新闻网站或数据源中收集 5 至 10 种新闻文本，根据课堂中讲解的 one-hot 特征提取方式，或自行查找其他文本特征提取方式，对文本进行特征提取和向量化，并利用机器学习模型进行分类，构建一个新闻文本分类系统。根据该要求，本项目最终确定 8 个新闻类别：财经、体育、科技、娱乐、社会、教育、军事、汽车。

## 1.2 项目与 AIGI-Holmes 的关系

本项目不是从零开始做一个孤立的分类页面，而是在已有 AIGI-Holmes 系统上进行扩展。AIGI-Holmes 原本是一个面向新闻媒体场景的 AI 生成图片检测系统，主要功能包括新闻图片上传检测、新闻 URL 图片抓取检测、批量图片检测、检测报告生成、用户系统和历史记录管理等。原系统关注的是“新闻中的图片是否可能由 AI 生成”，而本次大作业新增的新闻文本分类模块关注的是“新闻文本属于哪一类主题”。

因此，本项目的总体思路是：以新闻文本分类作为机器学习课程任务，以 AIGI-Holmes 作为工程载体，将文本分类能力集成到原有新闻内容审核平台中。这样既符合题目对新闻文本分类系统的要求，也能够体现实际工程中的系统集成能力。最终系统不仅可以检测新闻图片真实性，还可以对新闻标题和正文摘要进行自动分类，从而形成更完整的新闻图文内容分析流程。

## 1.3 主要研究方法

针对新闻文本分类任务，本项目采用了监督学习方法。整体流程包括数据收集、文本清洗、特征向量化、模型训练、模型评估和系统部署。具体来说，首先收集并整理带有类别标签的新闻文本数据；然后对不同来源的数据进行标签统一、去重和清洗；接着将新闻标题与正文摘要组合为模型输入，并使用 TF-IDF、词袋统计等方式将中文文本转换为数值特征；最后训练多种分类模型并比较实验结果。

在特征提取方面，题目中提到可以使用 one-hot 特征。one-hot 能够表示词语是否出现，但对于中文新闻文本来说，如果词汇量较大，one-hot 会造成维度很高且无法体现词语重要性。因此，本项目在 one-hot 思想的基础上进一步采用 TF-IDF 特征。TF-IDF 不仅能表示词语是否出现，还能根据词语在文本中的出现频率和在整个语料中的区分度赋予权重，更适合新闻分类任务。

在模型方面，本项目比较了传统机器学习模型和简单深度学习模型。传统机器学习模型包括朴素贝叶斯、逻辑回归和线性 SVM，这些模型训练速度快、部署成本低，适合文本分类基线实验。深度学习模型采用 TextCNN，用于比较神经网络方法在同一任务上的效果。通过多模型对比，可以更全面地分析不同方法的优缺点。

## 1.4 主要存在问题与解决思路

新闻文本分类任务主要存在以下几个问题。

第一，中文文本无法直接输入模型，需要进行向量化。中文没有天然空格分词，标题又通常较短，如果只使用普通词袋模型，容易丢失细粒度信息。针对该问题，本项目同时使用字符级 TF-IDF、词级 TF-IDF 和字符 Count 特征，从字符和词语两个层面提取信息。

第二，不同新闻类别之间可能存在语义交叉。例如，科技新闻中可能出现企业融资，容易与财经类混淆；社会新闻中可能包含教育、汽车、政策等内容，也容易与其他类别交叉。针对该问题，本项目采用 Macro F1 和混淆矩阵进行评估，不只看整体准确率，也分析不同类别的分类效果。

第三，不同数据源的类别标签不完全一致。例如有的数据源使用“IT”“互联网”等标签，而本项目类别中统一使用“科技”。针对该问题，本项目在数据预处理阶段建立了类别映射规则，将不同来源的标签统一映射为 8 个目标类别。

第四，课程作业不仅要求模型训练，还要求构建系统。因此，本项目将模型训练结果保存为工程产物，并通过 FastAPI 后端提供接口，再由前端页面调用接口展示分类结果，实现从算法实验到可用系统的完整闭环。

# 二、模型方法

## 2.1 任务定义

本项目的任务是多分类新闻文本分类。给定一条新闻标题和可选正文摘要，系统需要预测其所属类别。类别集合定义如下：

| 序号 | 类别 |
|---|---|
| 1 | 财经 |
| 2 | 体育 |
| 3 | 科技 |
| 4 | 娱乐 |
| 5 | 社会 |
| 6 | 教育 |
| 7 | 军事 |
| 8 | 汽车 |

模型输入形式为：

```text
新闻标题 + 新闻正文摘要
```

模型输出形式为：

```text
预测类别、置信度、各类别概率或得分、关键词
```

其中，预测类别是系统最终给用户展示的分类结果；置信度用于表示模型对当前预测的把握程度；各类别概率分布用于展示模型在不同类别之间的判断差异；关键词用于辅助解释模型为什么更倾向于某一类别。

## 2.2 文本预处理方法

在模型训练之前，需要先对原始文本进行预处理。本项目的数据预处理主要包括以下步骤。

第一步是文本清洗。系统会去除文本中的异常空白字符、BOM 字符和多余换行，将标题和正文统一转换为较规整的字符串形式。对于过长正文，只保留前若干字符作为摘要，避免输入过长导致噪声增加。

第二步是标签规范化。由于不同数据源中的标签名称不完全一致，本项目为每一类新闻建立了别名映射。例如，“IT”“互联网”“数码”“technology”等标签会统一映射为“科技”；“股票”“金融”“证券”“economy”等标签会统一映射为“财经”；“电影”“明星”“音乐”等标签会统一映射为“娱乐”。这样可以保证不同来源的数据最终进入同一套类别体系。

第三步是样本过滤和去重。系统会删除空标题、无效标签、过短文本和重复样本。去重时不仅比较类别，还会比较标题和正文摘要，防止同一条新闻重复进入训练集，影响模型评估结果。

第四步是训练集、验证集和测试集划分。项目使用分层划分思想，尽量保证每个类别在训练、验证、测试阶段都有样本。训练集用于模型拟合，验证集用于输入策略选择和 TextCNN 参数比较，测试集用于最终模型评价。

## 2.3 输入策略设计

新闻文本分类可以只使用标题，也可以使用标题加正文。标题通常包含新闻核心信息，但有时信息较短；正文摘要能提供更多上下文，但如果直接引入大量正文，也可能带来噪声。因此，本项目设计并比较了三种输入策略：

| 输入策略 | 含义 |
|---|---|
| title_only | 只使用新闻标题 |
| title_plus_summary | 使用标题和正文摘要 |
| title_weighted_content | 增强标题权重，同时拼接部分正文 |

实验结果如下：

| 输入策略 | Accuracy | Macro F1 |
|---|---:|---:|
| title_plus_summary | 0.7777 | 0.7786 |
| title_only | 0.7673 | 0.7678 |
| title_weighted_content | 0.7607 | 0.7613 |

从实验结果可以看出，`title_plus_summary` 的 Macro F1 最高，说明在标题基础上加入正文摘要能够提升模型对新闻主题的判断能力。而 `title_weighted_content` 并未取得更好效果，说明过度重复标题或加入过多正文片段可能引入冗余信息。最终系统选择 `title_plus_summary` 作为默认输入策略。

## 2.4 特征提取方法

本项目使用了三类文本特征，并将它们组合起来作为传统机器学习模型的输入。

第一类是字符级 TF-IDF 特征。中文新闻标题通常较短，且很多类别关键词可以通过连续字符体现，例如“央行”“股市”“女排”“芯片”“高考”“舰艇”等。字符级 TF-IDF 可以在不完全依赖分词准确性的情况下捕捉这些特征。本项目使用字符窗口提取 2 到 4 个字符的 n-gram 特征。

第二类是词级 TF-IDF 特征。项目使用 jieba 对中文文本进行分词，然后提取词级 n-gram 特征。词级特征能够更好地表达语义单位，例如“人工智能”“新能源车”“电信诈骗”“研究生考试”等。与字符特征相比，词级特征具有更强的语义解释性。

第三类是字符 Count 特征。Count 特征统计字符或字符组合出现次数，可以补充 TF-IDF 特征在词频统计方面的信息，使模型获得更丰富的文本表示。

相较于简单 one-hot 表示，TF-IDF 的优势在于能够体现词语的重要程度。如果一个词在某篇新闻中频繁出现，但在所有新闻中并不常见，则它对分类更有帮助。例如“舰艇”“演训”对军事类新闻具有较强区分度；“票房”“主演”对娱乐类新闻具有较强区分度；“融资”“汇率”对财经类新闻具有较强区分度。

## 2.5 传统机器学习模型

本项目训练并比较了三种传统机器学习模型。

第一种是朴素贝叶斯模型。朴素贝叶斯基于贝叶斯公式，假设特征之间相互独立，通过计算文本属于各类别的概率来完成分类。虽然独立性假设在真实文本中并不完全成立，但朴素贝叶斯在文本分类任务中通常表现稳定，训练速度快，适合作为文本分类基线模型。

第二种是逻辑回归模型。逻辑回归通过线性函数和概率映射实现分类，能够学习不同特征对类别的权重。对于高维稀疏文本特征，逻辑回归具有较强的泛化能力，并且训练结果相对容易解释。

第三种是线性 SVM。SVM 的目标是在高维特征空间中寻找能够最大化类别间隔的分类边界。文本特征通常是高维稀疏向量，因此线性 SVM 在文本分类中也是常见方法。本项目使用类别均衡权重，以缓解不同类别样本分布差异对模型的影响。

## 2.6 TextCNN 模型

除了传统机器学习模型，本项目还实现并训练了 TextCNN 模型，用于对比深度学习方法。TextCNN 的核心思想是将文本看作字符或词序列，先通过 Embedding 层将离散字符映射为向量，再使用不同大小的卷积核提取局部 n-gram 特征，最后通过最大池化和全连接层输出类别。

本项目中的 TextCNN 主要结构如下：

1. Embedding 层：将字符编号转换为低维向量；
2. 多组一维卷积层：使用不同 kernel size 提取局部文本模式；
3. ReLU 激活函数：增强模型非线性表达能力；
4. Max Pooling：保留每个卷积核检测到的最强特征；
5. Dropout：降低过拟合风险；
6. 全连接层：输出 8 个新闻类别的分类结果。

TextCNN 的优点是能够自动学习局部文本模式，不需要完全依赖人工设计的 TF-IDF 特征。缺点是训练成本更高，对数据规模和参数设置更敏感。在本项目实验中，TextCNN 的最终效果接近朴素贝叶斯，但没有明显超过传统模型，因此系统默认模型仍选择朴素贝叶斯，同时保留 TextCNN 作为对比实验结果。

## 2.7 评价指标

本项目使用以下指标评价模型效果：

| 指标 | 含义 |
|---|---|
| Accuracy | 所有样本中分类正确的比例 |
| Precision | 被预测为某类的样本中真正属于该类的比例 |
| Recall | 某类真实样本中被正确识别出来的比例 |
| F1-score | Precision 和 Recall 的调和平均 |
| Macro F1 | 各类别 F1 的平均值，能反映模型对各类别的整体表现 |
| 混淆矩阵 | 展示真实类别和预测类别之间的混淆情况 |

由于本项目是 8 分类任务，仅看 Accuracy 不够全面。如果某些类别效果很差，但其他类别数量较多，Accuracy 可能仍然较高。因此本项目重点关注 Macro F1。Macro F1 对每个类别一视同仁，更适合衡量多分类模型是否均衡。

# 三、系统设计

## 3.1 系统总体结构

本项目的系统整体结构可以分为数据层、模型层、后端服务层和前端展示层四个部分。

数据层负责新闻数据读取、清洗、类别统一和样本划分。模型层负责特征提取、模型训练、模型评估和模型保存。后端服务层基于 FastAPI 实现模型加载、单条分类、批量分类、实验指标读取和历史记录保存。前端展示层负责提供用户输入界面，并展示分类结果、概率分布、模型对比和实验分析。

系统整体流程如下：

```text
新闻数据收集
    ↓
数据清洗与标签统一
    ↓
标题和正文摘要组合
    ↓
文本特征提取与向量化
    ↓
训练多个分类模型
    ↓
测试集评估与模型选择
    ↓
保存模型文件和指标文件
    ↓
FastAPI 后端加载模型
    ↓
前端页面调用接口
    ↓
展示分类结果和实验分析
```

## 3.2 数据处理设计

数据处理是本项目工作量较大的部分。为了满足题目中“收集 5-10 种新闻文本”的要求，本项目最终确定了 8 个新闻类别。正式训练数据来自 NLPCC2017 和 HF-THUCNews/cnews 的混合数据源。

在数据整理过程中发现，单一数据源并不能完全覆盖所有目标类别。例如，NLPCC2017 中缺少教育类，而 HF-THUCNews/cnews 中可以补充教育类数据。因此项目采用混合数据源方案：以 NLPCC2017 中覆盖较好的类别为主体，再使用 HF-THUCNews/cnews 补充缺失类别。最终得到的正式训练数据规模为 20000 条，每个类别 2500 条。

类别分布如下：

| 类别 | 样本数 |
|---|---:|
| 财经 | 2500 |
| 体育 | 2500 |
| 科技 | 2500 |
| 娱乐 | 2500 |
| 社会 | 2500 |
| 教育 | 2500 |
| 军事 | 2500 |
| 汽车 | 2500 |
| 合计 | 20000 |

这样的均衡数据设计可以避免模型过度偏向样本数量较多的类别，也方便使用 Macro F1 对各类别进行公平评估。

## 3.3 模型训练流程设计

模型训练流程主要包括以下步骤。

第一步，读取数据源。系统支持读取本地 CSV、TXT、JSON、JSONL 等格式，也支持从公开数据集中整理样本。读取后统一转换为包含 category、title、content 字段的数据格式。

第二步，执行数据清洗。清洗内容包括去除空文本、过滤过短文本、统一类别名称、去除重复标题和正文摘要。清洗完成后统计每个类别的样本数量。

第三步，控制类别均衡。为了让训练集各类别数量一致，本项目设置每类最多 2500 条样本。这样可以保证最终训练数据中 8 个类别样本数相同。

第四步，划分训练集、验证集和测试集。训练集用于拟合模型，验证集用于比较输入策略和 TextCNN 参数，测试集用于最终评估模型性能。

第五步，比较输入策略。项目分别测试 title_only、title_plus_summary 和 title_weighted_content 三种输入策略，最终选择 Macro F1 最高的 title_plus_summary。

第六步，训练模型。传统模型使用组合特征进行训练，包括字符级 TF-IDF、词级 TF-IDF 和字符 Count 特征。深度学习模型 TextCNN 使用字符编号序列作为输入。

第七步，评估模型。系统在测试集上计算 Accuracy、Macro F1、分类报告和混淆矩阵，并保存到 YAML 指标文件中。

第八步，保存训练产物。训练完成后，系统保存模型文件，包括 nb.joblib、lr.joblib、svm.joblib、textcnn.pt，以及 metadata.yaml 和 metrics_full.yaml。后端服务启动时会读取这些产物。

## 3.4 后端接口设计

本项目后端基于 FastAPI 实现，并沿用 AIGI-Holmes 原有 `/api/...` 接口风格。新增新闻文本分类接口前缀为：

```text
/api/text-classify
```

主要接口如下：

| 接口 | 方法 | 功能 |
|---|---|---|
| /api/text-classify/models | GET | 获取模型列表、默认模型和训练指标 |
| /api/text-classify/experiments | GET | 获取实验分析结果、混淆矩阵和训练曲线 |
| /api/text-classify/predict | POST | 对单条新闻标题和正文进行分类 |
| /api/text-classify/batch | POST | 上传 CSV 文件进行批量分类 |
| /api/text-classify/history | GET | 查询登录用户的新闻分类历史 |

单条预测接口接收新闻标题、正文和模型选择参数。后端首先检查标题是否为空，然后根据用户选择加载对应模型，构造模型输入，得到各类别概率或得分，最后返回类别、置信度、概率分布、关键词和模型版本。

批量分类接口接收 CSV 文件，要求文件包含 title 和 content 两列。系统最多支持单次 100 条新闻批量分类，防止一次请求过大影响服务响应速度。批量分类结果可以在前端表格中展示，并支持导出。

历史记录接口只对登录用户开放。用户每次分类后，系统会保存标题、正文摘要、预测类别、置信度、概率分布、关键词、模型名称和模型版本，方便后续查看。

## 3.5 前端页面设计

前端新增“新闻文本分类”页面，并保持与 AIGI-Holmes 原有界面风格一致。页面主要包含以下部分：

第一部分是模型选择区域。用户可以选择不同模型，例如朴素贝叶斯、逻辑回归、线性 SVM 或 TextCNN。默认选择训练指标最好的模型。

第二部分是新闻输入区域。用户可以输入新闻标题和新闻正文摘要。系统以标题为主、正文摘要为辅进行分类。

第三部分是演示样例区域。前端提供若干条示例新闻，用户可以直接选择样例填入输入框，方便课堂演示。

第四部分是分类结果区域。系统会显示预测类别、置信度、各类别概率条和关键词。例如输入科技类新闻时，系统可能输出“科技”类别，并显示“大模型”“多模态”“图像问答”等关键词。

第五部分是模型对比和实验分析区域。前端会展示不同模型的 Accuracy 和 Macro F1，还会展示类别分布、输入策略对比、弱项类别、Top 混淆类别对、TextCNN 训练曲线、各类别 Precision/Recall/F1 和混淆矩阵。

第六部分是 CSV 批量分类区域。用户可以上传包含 title 和 content 两列的 CSV 文件，系统批量返回每条新闻的预测类别和置信度，并支持下载分类结果。

## 3.6 数据库存储设计

为了让系统不仅能即时分类，还能保存用户操作记录，本项目新增了新闻文本分类历史记录表。主要字段包括：

| 字段 | 含义 |
|---|---|
| user_id | 用户 ID |
| text_hash | 新闻文本哈希，用于标识文本 |
| title | 新闻标题 |
| content_preview | 新闻正文摘要 |
| category | 预测类别 |
| confidence | 置信度 |
| probs_json | 各类别概率 |
| keywords_json | 关键词 |
| model_key | 使用的模型 |
| model_version | 模型版本 |
| created_at | 创建时间 |

通过该表，用户可以在历史记录页面查看自己过去分类过的新闻文本，也可以按照类别或标题搜索记录。这部分体现了从算法模型到实际应用系统的工程化设计。

# 四、系统演示与分析

## 4.1 实验设置

本项目实验数据共 20000 条，分为 8 个类别，每类 2500 条。实验中主要进行了三类对比。

第一类是输入策略对比实验。比较 title_only、title_plus_summary 和 title_weighted_content 三种文本输入方式，选择最适合新闻分类的输入组织方式。

第二类是传统机器学习模型对比实验。比较朴素贝叶斯、逻辑回归和线性 SVM 在同一特征方案下的分类效果。

第三类是深度学习模型对比实验。训练 TextCNN 模型，并与传统机器学习模型进行比较。同时，TextCNN 部分还进行了两组参数尝试，包括不同最大文本长度、Embedding 维度、卷积核数量、Dropout 和训练轮数设置。

如果按实验组数统计，本项目至少完成了以下实验：

| 实验类型 | 实验内容 | 数量 |
|---|---|---:|
| 输入策略实验 | title_only、title_plus_summary、title_weighted_content | 3 组 |
| 传统模型实验 | 朴素贝叶斯、逻辑回归、线性 SVM | 3 组 |
| TextCNN 调参实验 | 两组 TextCNN 参数配置 | 2 组 |
| TextCNN 最终训练 | 选定较优参数后进行最终训练 | 1 组 |
| 合计 | 核心训练与对比实验 | 9 组 |

除模型训练实验外，本项目还进行了系统功能测试，包括单条新闻分类测试、CSV 批量分类测试、模型指标接口测试、实验分析展示测试和历史记录保存测试。

## 4.2 模型总体结果

最终模型对比结果如下：

| 模型 | Accuracy | Macro F1 |
|---|---:|---:|
| 朴素贝叶斯 | 0.8050 | 0.8053 |
| 逻辑回归 | 0.7883 | 0.7889 |
| 线性 SVM | 0.7707 | 0.7698 |
| TextCNN | 0.8043 | 0.8024 |

从实验结果可以看出，朴素贝叶斯模型在当前数据集上的 Accuracy 和 Macro F1 均为最高，因此被选为系统默认模型。TextCNN 的结果也较好，Macro F1 达到 0.8024，与朴素贝叶斯非常接近。逻辑回归和线性 SVM 也能够完成分类任务，但整体效果略低。

该结果说明，在本项目的数据规模和特征设计下，传统机器学习模型仍然具有较强竞争力。尤其是朴素贝叶斯模型，它与 TF-IDF 等文本特征结合后，能够快速有效地完成新闻分类。TextCNN 虽然具有自动学习特征的能力，但需要更多训练时间和参数调节，在当前实验条件下没有明显超过朴素贝叶斯。

## 4.3 各类别结果分析

以默认朴素贝叶斯模型为例，各类别 F1-score 如下：

| 类别 | F1-score |
|---|---:|
| 财经 | 0.7473 |
| 体育 | 0.8152 |
| 科技 | 0.7345 |
| 娱乐 | 0.8136 |
| 社会 | 0.6719 |
| 教育 | 0.9866 |
| 军事 | 0.8457 |
| 汽车 | 0.8278 |

从结果可以看出，教育类效果最好，F1-score 接近 0.99。这可能是因为教育类新闻中经常出现“高考”“招生”“学校”“研究生”“教师”等具有明显类别特征的词语。军事类和汽车类效果也较好，因为它们包含“舰艇”“防务”“演训”“新能源车”“驾驶”“车企”等领域词汇，类别边界相对清晰。

社会类效果相对较低，F1-score 为 0.6719。原因可能是社会类新闻主题范围很广，可能包含政策、民生、案件、交通、社区、市场监管等多种内容，与财经、娱乐、科技等类别存在一定交叉。例如，关于电信诈骗的新闻可能既有社会属性，也可能出现金融词汇；关于城市交通的新闻可能与汽车类存在联系。因此社会类更容易被其他类别混淆。

科技类和财经类的效果也有一定提升空间。科技新闻中经常出现企业、融资、产业、市场等词汇，容易与财经类发生混淆；财经新闻中也可能涉及科技企业或新能源汽车产业，因此模型在这些类别之间可能存在判断困难。

## 4.4 TextCNN 训练过程分析

TextCNN 模型训练过程中，验证集指标总体呈上升趋势。根据保存的训练记录，TextCNN 最终训练过程中 Macro F1 从第 1 轮的 0.6541 上升到第 9 轮的 0.8048，第 10 轮为 0.8024。训练曲线说明模型在前期能够持续学习新闻文本特征，后期提升速度逐渐变慢，说明模型已经接近当前配置下的收敛状态。

部分训练记录如下：

| Epoch | Loss | Val Accuracy | Val Macro F1 |
|---:|---:|---:|---:|
| 1 | 1.5280 | 0.6517 | 0.6541 |
| 3 | 0.8183 | 0.7603 | 0.7571 |
| 5 | 0.6062 | 0.7830 | 0.7818 |
| 7 | 0.4679 | 0.8007 | 0.8002 |
| 9 | 0.3584 | 0.8057 | 0.8048 |
| 10 | 0.3107 | 0.8043 | 0.8024 |

可以看出，随着训练轮数增加，loss 持续下降，验证集指标逐渐提高。但在第 9 轮以后，Macro F1 没有继续明显上升，说明继续增加训练轮数可能收益有限，甚至可能出现轻微过拟合。因此，本项目将 TextCNN 作为对比模型保留，而默认部署仍使用效果稳定、推理轻量的朴素贝叶斯模型。

## 4.5 系统演示结果

系统演示时，可以输入如下科技类新闻：

```text
标题：国产大模型发布新版本支持多模态理解
正文：新模型在图像问答和长文本处理方面提升明显，企业应用场景进一步拓展。
```

系统输出类别为“科技”，并返回较高置信度。关键词中会出现“大模型”“多模态”“模型”“版本”“企业应用”等词语，这些词与科技类新闻主题较为一致，说明模型能够根据文本中的主题词进行有效分类。

再例如输入体育类新闻：

```text
标题：中国女排逆转对手晋级四强
正文：关键局发球和拦网发挥出色，队员在落后时保持耐心完成反超。
```

系统会将其分类为“体育”。该新闻包含“女排”“晋级”“发球”“拦网”等明显体育特征词，因此模型能够较准确地识别类别。

如果输入社会类新闻：

```text
标题：警方破获一起电信诈骗案件
正文：犯罪团伙冒充客服诱导转账，涉案资金已部分追回。
```

系统通常会预测为“社会”。但这类新闻中也可能出现“资金”“转账”等财经相关词汇，因此从混淆矩阵角度看，社会类与财经类之间可能存在一定混淆。这也说明新闻文本分类不是简单关键词匹配，而需要模型综合判断上下文。

## 4.6 系统功能测试

本项目完成了以下系统功能测试。

第一，单条分类测试。用户在前端输入新闻标题和正文后，点击“开始分类”，系统能够返回预测类别、置信度、概率分布和关键词。

第二，模型切换测试。用户可以在前端选择不同模型，比较朴素贝叶斯、逻辑回归、线性 SVM 和 TextCNN 的预测结果。

第三，CSV 批量分类测试。用户上传包含 title 和 content 两列的 CSV 文件后，系统可以批量返回每条新闻的分类结果，并支持下载 CSV 结果文件。

第四，模型指标展示测试。前端能够读取后端模型指标接口，显示各模型 Accuracy、Macro F1、训练样本数、类别分布、弱项类别和混淆矩阵。

第五，历史记录测试。登录用户完成分类后，系统能够保存分类记录，并在历史记录页面按照类别或标题进行查询。

## 4.7 存在问题与改进方案

虽然本项目已经实现了新闻文本分类系统，但仍然存在一些不足。

第一，数据来源还可以继续扩展。当前数据主要来自公开新闻分类数据集，虽然已经覆盖 8 个类别，但真实新闻网站中的语言风格可能更加复杂。后续可以进一步爬取最新新闻网站数据，增强模型泛化能力。

第二，社会类新闻分类效果相对较低。原因是社会类本身范围较广，容易与财经、教育、汽车等类别交叉。后续可以细分社会类，或增加更高质量的社会类样本。

第三，当前默认模型仍然是传统机器学习模型。虽然朴素贝叶斯效果较好，但语义理解能力有限。后续可以尝试 BERT、RoBERTa、MacBERT 等预训练语言模型，进一步提升复杂语义场景下的分类准确率。

第四，当前系统主要做文本主题分类，还没有与图片检测结果进行深层融合。后续可以将文本分类结果与 AIGI-Holmes 原有图片真实性检测、CLIP 图文一致性检测结合起来，形成更完整的新闻可信度评估。

# 五、对本门课的感想、意见和建议

通过本次机器学习大作业，我对机器学习项目的完整流程有了更深入的理解。以前学习机器学习算法时，更多关注模型公式和课堂示例，例如分类、回归、特征提取等概念。但在真正完成一个工程项目时，我发现模型训练只是其中一部分，前期的数据处理、特征设计、实验对比、结果分析和系统集成同样重要。

在本项目中，我首先需要理解题目要求，将“新闻文本分类”转化为具体工程任务；然后确定类别体系，整理数据源，处理标签不一致和类别缺失问题；接着进行文本清洗、特征提取和模型训练；最后还要把模型接入后端接口和前端页面，使用户能够真正使用这个模型。这个过程让我体会到，机器学习不是简单调用一个算法，而是一个从数据到模型再到系统应用的完整链条。

本次项目中，我也更直观地理解了不同模型的特点。朴素贝叶斯虽然理论假设较简单，但在文本分类任务中效果很好，训练和推理速度也很快；逻辑回归和线性 SVM 对高维稀疏特征也有较强适应能力；TextCNN 能够自动学习局部文本模式，但需要更多训练时间和参数调节。通过这些实验对比，我认识到模型选择并不是越复杂越好，而是要结合数据规模、任务特点、部署成本和实际效果综合考虑。

此外，本项目让我认识到评价指标的重要性。如果只看 Accuracy，可能无法发现某些类别分类效果较差的问题。而 Macro F1 和混淆矩阵能够帮助分析不同类别的表现，发现社会类、财经类、科技类之间的混淆情况。这对后续改进模型非常有帮助。

对于课程学习，我认为机器学习课程如果能结合更多工程实践，会更容易理解算法的实际价值。课堂上学习算法原理是基础，而大作业则能帮助学生把算法应用到真实问题中。通过这次项目，我不仅复习了文本特征提取、分类模型和模型评价指标，也练习了如何将模型部署到实际系统中。

后续如果继续完善本项目，我计划从三个方向改进。第一，继续扩大新闻数据集，尤其是增加容易混淆类别的样本；第二，引入预训练语言模型，提高模型对复杂语义的理解能力；第三，将新闻文本分类结果与原有图片 AI 生成检测结果结合，进一步构建图文联合的新闻真实性分析系统。

# 六、项目工作量总结

为了更清晰地说明本项目的完成情况，现将主要工作量总结如下。

## 6.1 数据处理工作量

本项目整理了 8 类新闻文本数据，最终形成 20000 条均衡训练样本。数据处理过程中完成了多格式数据读取、标签统一、文本清洗、去重、类别均衡和训练集划分等工作。相比直接使用现成数据训练，本项目在数据整理和工程化处理上投入了较多工作。

## 6.2 模型实验工作量

本项目进行了多组模型实验。输入策略方面比较了 3 种文本组织方式；传统模型方面训练了朴素贝叶斯、逻辑回归和线性 SVM；深度学习方面训练了 TextCNN，并进行了两组参数尝试和最终训练。实验过程中保存了模型文件、训练指标、分类报告、混淆矩阵和 TextCNN 训练曲线。

## 6.3 系统开发工作量

本项目新增了新闻文本分类后端服务，包括模型加载、单条分类、批量分类、模型指标读取、实验分析读取和历史记录保存接口。前端新增了新闻文本分类页面，支持模型选择、示例填入、分类结果展示、概率分布展示、关键词展示、CSV 批量分类、模型对比展示、实验分析展示和历史记录查看。

## 6.4 工程集成工作量

本项目没有将新闻文本分类做成独立脚本，而是集成到 AIGI-Holmes 原有系统中。新增模块保持了原系统的 FastAPI 后端结构、前端标签页风格、用户登录和历史记录机制，使新闻文本分类成为原有新闻内容审核系统的一部分。这体现了从机器学习实验到完整工程系统的转化。

# 七、结论

本项目围绕“新闻文本分类”题目，完成了一个较完整的机器学习工程系统。项目从新闻数据收集和整理开始，经过文本清洗、特征提取、模型训练、实验对比和评估，最终将训练好的模型集成到 AIGI-Holmes 系统中，形成可交互的新闻文本分类功能。

实验结果表明，在当前数据集和特征设计下，朴素贝叶斯模型取得最佳综合效果，Accuracy 为 0.8050，Macro F1 为 0.8053。TextCNN 模型也取得接近效果，说明深度学习方法具有进一步提升空间。系统最终支持单条新闻分类、CSV 批量分类、模型指标展示、实验分析展示和历史记录保存，满足题目中“构建一个新闻文本分类系统”的要求。

总体来看，本项目不仅完成了机器学习模型训练，也完成了模型工程化部署和前端可视化展示，体现了较完整的课程实践过程。
