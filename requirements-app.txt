# Minimal dependencies for running the AIGI-Holmes detection app (app.py)
gradio>=4.0,<6.0
torch>=2.0
torchvision>=0.15
Pillow>=9.0
requests>=2.28

# Desktop-window launcher (desktop_app.py) — Windows native window via pywebview
pywebview>=4.0